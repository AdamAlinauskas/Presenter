{
    "version": "0.1.0",
    "command": "dotnet",
    "isShellCommand": true,
    "args": [],
    "tasks": [
        {
            "taskName": "build",
            "command": "${workspaceFolder}/scripts/run_dev",
            "args": ["-d"],
            "showOutput": "always",
            "options": {
                "cwd": "${workspaceFolder}/scripts"
            }
        }
    ]
}